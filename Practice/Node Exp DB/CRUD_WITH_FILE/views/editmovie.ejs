<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Advanced Movie Entry</title>
  <script src="https://unpkg.com/@tailwindcss/browser@4"></script>
</head>
<body class="bg-slate-100 flex items-center justify-center min-h-screen p-4 md:p-10">

  <form action="/editmovie/<%-data._id%>" method="POST" id="movieEntryForm" name="movieEntryForm"  
    class="bg-white p-8 rounded-3xl shadow-2xl w-full max-w-2xl space-y-6 border border-slate-200">
    
    <header class="border-b border-slate-100 pb-4">
      <h2 class="text-3xl font-black text-slate-900 tracking-tight">Update Movie Details</h2>
      <p class="text-slate-500">Fill in the comprehensive details for the film database.</p>
    </header>

    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
      
      <div class="md:col-span-2">
        <label for="movieTitle" class="block text-sm font-bold text-slate-700 mb-1">Update Movie Title</label>
        <input type="text" id="movieTitle" name="movieTitle" placeholder="The Dark Knight" required
            value="<%-data.movieTitle%>"
          class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none transition">
      </div>

      <div>
        <label for="director" class="block text-sm font-bold text-slate-700 mb-1">Update Director</label>
        <input type="text" id="director" name="director" placeholder="Christopher Nolan"
      value="<%-data.director%>"
          class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none">
      </div>

      <div>
        <label for="genre" class="block text-sm font-bold text-slate-700 mb-1"> Update Genre</label>
        <select id="genre" name="genre" class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none bg-white">
          <option value="action" <%-data.genre === 'action' ? "selected" : '' %> >Action</option>
          <option value="comedy"<%-data.genre === 'comedy' ? "selected" : '' %> >Comedy</option>
          <option value="drama" <%-data.genre === 'drama' ? "selected" : '' %> >Drama</option>
          <option value="sci-fi" <%-data.genre === 'sci-fi' ? "selected" : '' %> >Sci-Fi</option>
          <option value="horror" <%-data.genre === 'horror' ? "selected" : '' %> >Horror</option>
        </select>
      </div>

      <div>
        <label for="releaseDate" class="block text-sm font-bold text-slate-700 mb-1">Update Release Date</label>
        <input type="date" id="releaseDate" name="releaseDate" required
        value="<%-data.releaseDate %>"
          class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none">
      </div>

      <div>
        <label for="boxOfficeStatus" class="block text-sm font-bold text-slate-700 mb-1">Update Performance</label>
        <select id="boxOfficeStatus" name="boxOfficeStatus" class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none bg-white">
          <option value="theaters"  <%-data.boxOfficeStatus === 'theaters' ? "selected" : '' %>>In Theaters</option>
          <option value="blockbuster" <%-data.boxOfficeStatus === 'blockbuster' ? "selected" : '' %>>Blockbuster</option>
          <option value="average" <%-data.boxOfficeStatus === 'average' ? "selected" : '' %>>Average</option>
          <option value="flop" <%-data.boxOfficeStatus === 'flop' ? "selected" : '' %>>Flop</option>
        </select>
      </div>

      <div>
        <label for="duration" class="block text-sm font-bold text-slate-700 mb-1">Update Duration (mins)</label>
        <input type="number" id="duration" name="duration" placeholder="120"
        value="<%-data.duration %>"
          class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none">
      </div>

      <div>
        <label for="rating" class="block text-sm font-bold text-slate-700 mb-1">Update Rating (1-10)</label>
        <input type="range" id="rating" name="rating" min="1" max="10" step="0.1" 
        value="<%-data.rating %>"
          class="w-full h-2 bg-gray-200 rounded-lg appearance-none cursor-pointer accent-rose-500 mt-4">
      </div>
    </div>

    <div>
      <label for="synopsis" class="block text-sm font-bold text-slate-700 mb-1">Update Synopsis / Storyline</label>
      <textarea id="synopsis" name="synopsis" rows="3" placeholder="Briefly describe the movie plot..."
       value="<%-data.synopsis %>"
        class="w-full px-4 py-2.5 border border-slate-300 rounded-xl focus:ring-2 focus:ring-rose-500 focus:outline-none"></textarea>
    </div>

    <div>
      <label class="block text-sm font-bold text-slate-700 mb-1">Update Movie Poster</label>
      <div class="group relative border-2 border-dashed border-slate-300 rounded-2xl p-6 transition hover:bg-slate-50 hover:border-rose-400 text-center">
        <input type="file" id="posterUpload" name="posterUpload" accept="image/*" 
          class="absolute inset-0 w-full h-full opacity-0 cursor-pointer">
        <div class="space-y-2">
          <span class="text-rose-500 font-medium">Click to upload poster</span>
          <p class="text-xs text-slate-400 uppercase tracking-widest">SVG, PNG, JPG (MAX 800x1200px)</p>
        </div>
      </div>
    </div>

    <div class="flex gap-4 pt-4">
   
      <button type="submit" id="submitForm" name="submitForm" 
        class="flex-[2] bg-rose-500 text-white px-6 py-3 rounded-xl font-bold shadow-lg shadow-rose-200 hover:bg-rose-600 transition active:scale-[0.98]">
        Publish Movie
      </button>
    </div>
  </form>

</body>
</html>